milestone = "R08"
status = "active"
minimum_percent_when_done = 96.0
target_percent_when_done = 100.0

# R08 makes Rust a first-class SDK-style project in the dotnet toolchain.
# The denominator is declared up front from the roadmap DoD. A contract remains false
# until executable evidence proves the supported behavior on the required platforms.

[[contracts]]
id = "FW-R08-SDK-001"
family = "dotnet new rust creates a structurally valid Rust/.NET project"
implemented = true
proof = "tests/r08_dotnet_new.rs installs the repository template through the real dotnet template engine, creates HelloFerrum with 'dotnet new rust', and verifies the generated .rsproj and Rust source. CI certification is still pending for this exact SHA."

[[contracts]]
id = "FW-R08-SDK-002"
family = ".rsproj is an SDK-style project owned by FerrumWeave.Sdk"
implemented = false
proof = ""

[[contracts]]
id = "FW-R08-SDK-003"
family = "dotnet restore performs the supported restore responsibilities"
implemented = false
proof = ""

[[contracts]]
id = "FW-R08-SDK-004"
family = "dotnet build produces the managed FerrumWeave assembly"
implemented = false
proof = ""

[[contracts]]
id = "FW-R08-SDK-005"
family = "dotnet run executes the generated Rust project"
implemented = false
proof = ""

[[contracts]]
id = "FW-R08-SDK-006"
family = "dotnet clean removes supported build outputs predictably"
implemented = false
proof = ""

[[contracts]]
id = "FW-R08-SDK-007"
family = "dotnet test has a documented supported hook or an explicit executable limitation"
implemented = false
proof = ""

[[contracts]]
id = "FW-R08-SDK-008"
family = "the project lifecycle requires no bespoke manual build script outside the SDK contract"
implemented = false
proof = ""

[[contracts]]
id = "FW-R08-SDK-009"
family = "the SDK defaults to the current supported stable/LTS .NET line"
implemented = false
proof = ""

[[contracts]]
id = "FW-R08-SDK-010"
family = "the complete supported R08 SDK lifecycle is certified from documented prerequisites on Linux and Windows"
implemented = false
proof = ""
