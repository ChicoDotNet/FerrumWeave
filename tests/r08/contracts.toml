milestone = "R08"
status = "done"
minimum_percent_when_done = 96.0
target_percent_when_done = 100.0

# R08 makes Rust a first-class SDK-style project in the dotnet toolchain.
# The denominator is declared up front from the roadmap DoD. A contract remains false
# until executable evidence proves the supported behavior on the required platforms.
# 2026-09 backend-convergence audit: the product build contract additionally requires
# .rsproj -> FerrumWeave.Sdk -> rustc -> FerrumWeave CodegenBackend causality; the legacy
# ferrumweave_emit/source-parser path is not valid product evidence.

[[contracts]]
id = "FW-R08-SDK-001"
family = "dotnet new rust creates a structurally valid Rust/.NET project"
implemented = true
proof = "tests/r08_dotnet_new.rs installs the repository template through the real dotnet template engine, creates HelloFerrum with 'dotnet new rust', and verifies the generated .rsproj and Rust source. Certified on Linux and Windows by Rust CI #220 at d294ce796e81372ab49db36cc7f526d840410f01."

[[contracts]]
id = "FW-R08-SDK-002"
family = ".rsproj is an SDK-style project owned by FerrumWeave.Sdk"
implemented = true
proof = "sdk/templates/rust/HelloFerrum.rsproj names FerrumWeave.Sdk as its MSBuild SDK. Current sdk/FerrumWeave.Sdk/Sdk/Sdk.targets owns compilation, builds compiler/codegen-backend and invokes rustc with -Z codegen-backend=<FerrumWeave>; standard .NET lifecycle remains delegated to Microsoft.NET.Sdk. The exact product-path replay is covered by FW-R08-SDK-011."

[[contracts]]
id = "FW-R08-SDK-003"
family = "dotnet restore performs the supported restore responsibilities"
implemented = true
proof = "tests/r08_restore.rs runs real 'dotnet restore HelloFerrum.rsproj' with the repository SDK resolver path and requires standard obj/project.assets.json output. Certified on Linux and Windows by Rust CI #225 at 8cdd02e2d1d0cdf7f06b580b21c5c6ebe8ec369d."

[[contracts]]
id = "FW-R08-SDK-004"
family = "dotnet build produces the managed FerrumWeave assembly"
implemented = true
proof = "tests/r08_build.rs runs real 'dotnet build HelloFerrum.rsproj' through FerrumWeave.Sdk and verifies the managed output. The stronger backend-convergence certifier tests/sdk_backend_path.rs proves that the same normal dotnet build is gated by real rustc, contains no ferrumweave_emit product path, and that Rust-only mutation 137 -> 211 changes both managed assembly bytes and an independent CoreCLR consumer observable. Exact product-path replay SHA 9b08fa42e0a0c5ad12700b544a670aabc045569c passed Rust CI #565 on Ubuntu and Windows, with backend convergence #202, R02 #241, R03 #268 and R04 #417 GREEN."

[[contracts]]
id = "FW-R08-SDK-005"
family = "dotnet run executes the generated Rust project"
implemented = true
proof = "tests/r08_run.rs invokes real 'dotnet run --project HelloFerrum.rsproj' through FerrumWeave.Sdk, requires the managed entry point to execute the Rust source observable 'Hello from FerrumWeave!', and preserves the managed assembly contract. Certified on Linux and Windows by Rust CI #251 and R04 CLR CTS Reflection #146 at 9b19d642f92d0dfe04000c1dc0c48f8311f53bbe."

[[contracts]]
id = "FW-R08-SDK-006"
family = "dotnet clean removes supported build outputs predictably"
implemented = true
proof = "tests/r08_clean.rs performs the supported real build lifecycle, requires bin/Debug/net10.0/HelloFerrum.dll to exist, invokes real 'dotnet clean HelloFerrum.rsproj' through FerrumWeave.Sdk, and requires that managed build artifact to be removed. Certified on Linux and Windows by Rust CI #253 and R04 CLR CTS Reflection #148 at 902a7c53557e3cf4de185a9460f4664487f53fd7."

[[contracts]]
id = "FW-R08-SDK-007"
family = "dotnet test has a documented supported hook or an explicit executable limitation"
implemented = true
proof = "tests/r08_test.rs invokes real 'dotnet test HelloFerrum.rsproj --no-restore' through FerrumWeave.Sdk and requires the SDK-owned VSTest target to fail explicitly with the documented limitation that no Rust test adapter is provided yet. Certified on Linux and Windows by Rust CI #256 and R04 CLR CTS Reflection #151 at e4405558f1e3f89e673c6fcd7ecb2e1b1e47be5f."

[[contracts]]
id = "FW-R08-SDK-008"
family = "the project lifecycle requires no bespoke manual build script outside the SDK contract"
implemented = true
proof = "tests/r08_sdk_lifecycle.rs rejects bespoke shell/PowerShell/cmd/batch/Makefile build scripts in the canonical Rust template and requires lifecycle ownership in FerrumWeave.Sdk. The current SDK directly builds and invokes the FerrumWeave rustc CodegenBackend; tests/sdk_backend_path.rs independently rejects ferrumweave_emit in the product compilation path. Exact product-path replay SHA 9b08fa42e0a0c5ad12700b544a670aabc045569c passed portable Rust CI #565."

[[contracts]]
id = "FW-R08-SDK-009"
family = "the SDK defaults to the current supported stable/LTS .NET line"
implemented = true
proof = "tests/r08_supported_dotnet.rs requires the canonical template to target net10.0 and verifies the certified environment exposes a .NET 10 SDK through the real 'dotnet --list-sdks' command. Certified on Linux and Windows by Rust CI #260 and R04 CLR CTS Reflection #155 at 679cf6f7152ff647673cf94ee8fc1b490e26f438."

[[contracts]]
id = "FW-R08-SDK-010"
family = "the complete supported R08 SDK lifecycle is certified from documented prerequisites on Linux and Windows"
implemented = true
proof = "tests/r08_end_to_end.rs exercises the complete real dotnet lifecycle, while tests/sdk_backend_path.rs certifies the compiler causality inside that SDK boundary. Exact product-path replay SHA 9b08fa42e0a0c5ad12700b544a670aabc045569c completed Rust CI #565 GREEN for fmt/clippy, functional/source-causality, code coverage and integration on Ubuntu and Windows; Backend convergence #202, R02 #241, R03 #268 and R04 #417 were also GREEN."

[[contracts]]
id = "FW-R08-SDK-011"
family = "normal .rsproj compilation causally invokes rustc with the FerrumWeave CodegenBackend rather than a legacy emitter/source parser"
implemented = true
proof = "sdk/FerrumWeave.Sdk/Sdk/Sdk.targets builds compiler/codegen-backend and invokes rustc -Z codegen-backend=<FerrumWeave>. tests/sdk_backend_path.rs first supplies malformed Rust that the legacy source parser could have accepted and requires a real rustc syntax diagnostic plus no DLL and no ferrumweave_emit trace. It then mutates only valid Rust source 137 -> 211 and requires both HelloFerrum.dll bytes and an independent C# CoreCLR observable to change. Exact SHA 9b08fa42e0a0c5ad12700b544a670aabc045569c passed Rust CI #565 on both Ubuntu and Windows, with FerrumWeave backend convergence #202, R02 #241, R03 #268 and R04 #417 GREEN."
