milestone = "R07"
status = "done"
minimum_percent_when_done = 96.0
target_percent_when_done = 100.0

# R07 turns mechanical CLR interoperability into explicit cross-runtime semantics.
# The denominator is declared up front from the roadmap and inherited R04/R06 boundaries.
# A contract remains false until executable evidence proves the supported behavior; ADRs and
# compatibility documentation are milestone DoD evidence but do not substitute for execution.
# 2026-09 backend-convergence audit: product claims require Rust source -> rustc -> FerrumWeave
# CodegenBackend causal evidence. Historical emitter evidence remains characterization only.

[[contracts]]
id = "FW-R07-SEM-001"
family = "Option<T> Some for a supported managed reference preserves the managed value/identity at the CLR boundary"
implemented = true
proof = "tests/r07_option_reference.rs builds real Rust Option<&'static str> source through .rsproj -> rustc -> FerrumWeave CodegenBackend -> FerrumWeave MIR lowering -> FerrumWeave CIL/metadata, then executes an independent net10.0 C# consumer on CoreCLR. The certifier mutates only the Rust Some payload from FerrumWeave to FerrumWeave-mutated and requires both managed artifact bytes and the CLR observable to change. Exact SHA 47d83fd2b27c77f8ee5f662860866494c5a6d33d was GREEN in Rust CI #508 and FerrumWeave codegen backend convergence #145, with R02 #184, R03 #211 and R04 #360 also GREEN."

[[contracts]]
id = "FW-R07-SEM-002"
family = "Option<T> None for a supported managed reference projects to explicit CLR null semantics"
implemented = true
proof = "tests/r07_option_reference.rs builds real Rust Option<&'static str> source through .rsproj -> rustc -> FerrumWeave CodegenBackend and requires the independent C# consumer to observe Rust None as CLR null while the Some payload remains Rust-source causal. Exact SHA 47d83fd2b27c77f8ee5f662860866494c5a6d33d was GREEN in Rust CI #508 and FerrumWeave codegen backend convergence #145 on the portable lane, with R02 #184, R03 #211 and R04 #360 also GREEN."

[[contracts]]
id = "FW-R07-SEM-003"
family = "Option<T> for a supported CLR value type uses an explicit System.Nullable<T> projection for Some and None"
implemented = true
proof = "tests/r07_option_value.rs builds real Rust Option<i32> source through .rsproj/MSBuild -> rustc -> FerrumWeave CodegenBackend -> FerrumWeave MIR lowering -> FerrumWeave CTS/CIL/metadata, mutates only Some(42) -> Some(73), requires managed artifact bytes and the independent C# observable to change, and requires None to remain Nullable<int> without value. Exact SHA e0d411ca4b3fe2024810a5fd2a5dced7676100a6 was GREEN in Rust CI #515 and FerrumWeave codegen backend convergence #152, with R02 #191, R03 #218 and R04 #367 also GREEN."

[[contracts]]
id = "FW-R07-SEM-004"
family = "Result<T, E> success crosses a supported public CLR boundary as the declared success value"
implemented = true
proof = "tests/r07_result_success.rs builds real Rust Result<i32, i32>::Ok source through .rsproj/MSBuild -> rustc -> FerrumWeave CodegenBackend -> FerrumWeave MIR lowering -> FerrumWeave CIL/metadata, mutates only Ok(42) -> Ok(73), requires managed artifact bytes and the independent CoreCLR observable to change, and exposes the success payload through the declared public CLR method. Exact SHA d47272f467ec36bb4d2dba3dc585d09481209ff9 was GREEN in Rust CI #523 and FerrumWeave codegen backend convergence #160, with R02 #199, R03 #226 and R04 #375 also GREEN."

[[contracts]]
id = "FW-R07-SEM-005"
family = "Result<T, E> failure crosses a supported public CLR boundary as a coherent managed exception rather than a hidden status-code convention"
implemented = true
proof = "tests/r07_result_failure.rs builds real Rust Result<i32, i32>::Err source through .rsproj/MSBuild -> rustc -> FerrumWeave CodegenBackend -> FerrumWeave MIR lowering -> FerrumWeave CIL/metadata, mutates only Err(42) -> Err(73), requires managed artifact bytes and the independent CoreCLR exception observable to change, and requires the CLR boundary to represent failure as InvalidOperationException carrying the Rust-derived payload rather than a hidden status code. Exact SHA 1fa3bf19237d77dac15c8357de9041801aba3055 was GREEN in Rust CI #527 and FerrumWeave codegen backend convergence #164, with R02 #203, R03 #230 and R04 #379 also GREEN."

[[contracts]]
id = "FW-R07-SEM-006"
family = "panic behavior at a supported managed public boundary follows an explicit policy and does not silently unwind across an unsupported runtime boundary"
implemented = true
proof = "tests/r07_panic_boundary.rs builds real Rust through .rsproj/MSBuild -> rustc -> FerrumWeave CodegenBackend. A public answer() containing panic! is rejected before artifact creation with stable diagnostic FERRUMWEAVE_PANIC_BOUNDARY_REJECTED; then only Rust source is mutated to a supported answer() -> 42 and the same project must build a managed DLL. Exact SHA d90d1606419e9fec77e0846a1e978614cde394b7 completed all GitHub checks GREEN, including FerrumWeave rustc boundary on Ubuntu and Windows plus the R02-R04 replay lanes. This hardening falsifies an always-rejecting or generic unsupported-MIR implementation."

[[contracts]]
id = "FW-R07-SEM-007"
family = "a supported Rust Drop/RAII resource projects to IDisposable-style cleanup with exactly-once observable release semantics"
implemented = true
proof = "tests/r07_disposable.rs builds real Rust defining RustResource + Drop through .rsproj/MSBuild -> rustc -> FerrumWeave CodegenBackend -> disposable_resource_lowering -> disposable_resource_export, then an independent C# consumer observes IDisposable cleanup. The certifier mutates only Rust source from (seed=0, drop increment=1) to (seed=5, drop increment=2), requires managed artifact bytes to change, and requires CoreCLR to observe 0 -> 1 -> 1 and 5 -> 7 -> 7 so construction, cleanup increment and exactly-once/idempotent Dispose semantics are source-causal. Exact SHA 3ce6e50b85c57c8e5391e66fc4a8138fba0c1e97 was GREEN in Rust CI #542 and FerrumWeave codegen backend convergence #179, with R02 #218, R03 #245 and R04 #394 also GREEN."

[[contracts]]
id = "FW-R07-SEM-008"
family = "supported managed ownership/GC lifetime preserves identity without duplicate release or invalid access after release in supported scenarios"
implemented = true
proof = "tests/r07_managed_identity.rs builds RustResource + Drop from real Rust through .rsproj/MSBuild -> rustc -> FerrumWeave CodegenBackend, aliases the resulting managed object in an independent C# consumer, verifies ReferenceEquals identity, one shared exactly-once Dispose state and GC.KeepAlive lifetime, and mutates only the Rust constructor seed 0 -> 5 so both managed artifact bytes and the CoreCLR state observable change while identity remains true. Exact SHA 4718d1046ce3ed4a67aa286e54a068d56abd9102 was GREEN in Rust CI #545 and FerrumWeave codegen backend convergence #182, with R02 #221, R03 #248 and R04 #397 also GREEN on the portable replay."

[[contracts]]
id = "FW-R07-SEM-009"
family = "at least one supported generic boundary has a coherent Rust/CTS representation observable by CLR consumers"
implemented = true
proof = "tests/r07_generic_boundary.rs builds real Rust Option<i32> Some/None through .rsproj/MSBuild -> rustc -> FerrumWeave CodegenBackend -> FerrumWeave MIR lowering -> CTS/CIL/metadata, requires reflection to observe System.Nullable<> with System.Int32, executes Some/None on CoreCLR, and mutates only Some(42) -> Some(73) so both artifact bytes and the CLR observable change. Exact SHA dc960770fc675e7073b70b0bdf1aa6a469278089 was GREEN in Rust CI #548 and FerrumWeave codegen backend convergence #185, with R02 #224, R03 #251 and R04 #400 also GREEN. The earlier write_r06_static_api_artifact evidence remains characterization only and is not part of this PASS."

[[contracts]]
id = "FW-R07-SEM-010"
family = "Rust borrow/lifetime semantics that cannot safely escape a managed boundary are rejected explicitly rather than erased into GC reachability"
implemented = true
proof = "tests/r07_borrow_boundary.rs builds real Rust shared and exclusive borrow exports through .rsproj/MSBuild -> rustc -> FerrumWeave CodegenBackend and requires stable FERRUMWEAVE_BORROW_BOUNDARY_REJECTED diagnostics with no managed DLL; it then mutates only Rust source to an owned i32 boundary and requires successful artifact production. Rich FerrumWeave lowerers claim supported internal borrows before the escaping-borrow policy runs, preventing false rejection of valid managed projections. Exact SHA f2d3126c02b428454c3f3c4bc18c95cd15645456 was GREEN in Rust CI #553 and FerrumWeave codegen backend convergence #190, with R02 #229, R03 #256 and R04 #405 also GREEN on the portable replay. Historical projection/types evidence remains characterization only."

[[contracts]]
id = "FW-R07-SEM-011"
family = "unsupported cross-runtime semantic combinations fail explicitly and diagnostically"
implemented = true
proof = "tests/r07_unsupported_semantics.rs builds real Rust through .rsproj/MSBuild -> rustc -> FerrumWeave CodegenBackend and requires stable FERRUMWEAVE_SEMANTIC_BOUNDARY_REJECTED diagnostics with no managed DLL for unsupported char (including a non-BMP scalar) and String boundaries; it then mutates only Rust source to a supported owned i32 export and requires artifact production, falsifying reject-all behavior. semantic_boundary inspects monomorphized rustc types/MIR and identifies String through the rustc lang item rather than source/path matching. Exact SHA 6e86017e4b46526eec76a3f862a7bc922af14487 was GREEN in Rust CI #561 and FerrumWeave codegen backend convergence #198, with R02 #237, R03 #264 and R04 #413 also GREEN. Historical projection/types evidence remains characterization only."

[[contracts]]
id = "FW-R07-SEM-012"
family = "all supported R07 semantic-interoperability scenarios are certified on Linux and Windows"
implemented = true
proof = "The backend-convergence audit replaced projection/emitter-only claims with Rust-source-causal FerrumWeave CodegenBackend evidence for FW-R07-SEM-001 through FW-R07-SEM-011. Exact aggregate SHA 6e86017e4b46526eec76a3f862a7bc922af14487 completed GREEN in Rust CI #561 on both ubuntu-latest and windows-latest, with fmt+clippy, functional/source-causality guardrails, integration tests and coverage GREEN; FerrumWeave codegen backend convergence #198, R02 #237, R03 #264 and R04 #413 also closed GREEN. Historical milestone SHA ca0d589c4e673825c05b44d3bf1f7f6aa2d87478 remains historical evidence, not the product-path certification basis."
