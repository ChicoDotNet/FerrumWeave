name: FerrumWeave external managed assembly convergence

on:
  push:
    branches:
      - dev
      - main
    paths:
      - ".github/workflows/external-managed-assembly-convergence.yml"
      - "compiler/codegen-backend/**"
      - "compiler/cil/**"
      - "projection/**"
      - "eng/verification/verify_external_managed_assembly_backend.py"
      - "tests/r05/contracts.toml"
      - "Cargo.toml"
  pull_request:
    branches:
      - dev
      - main
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - ".github/workflows/external-managed-assembly-convergence.yml"
      - "compiler/codegen-backend/**"
      - "compiler/cil/**"
      - "projection/**"
      - "eng/verification/verify_external_managed_assembly_backend.py"
      - "tests/r05/contracts.toml"
      - "Cargo.toml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: external-managed-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  external-managed:
    name: External managed assembly (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            backend: libferrumweave_codegen_backend.so
          - os: windows-latest
            backend: ferrumweave_codegen_backend.dll
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-dotnet@v6.0.0
        with:
          dotnet-version: "10.0.400"

      - name: Install pinned rustc-private toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install nightly-2025-10-14 \
            --profile minimal \
            --component rustc-dev,rust-src,llvm-tools-preview
          rustc +nightly-2025-10-14 --version --verbose

      - name: Build FerrumWeave codegen backend
        shell: bash
        run: |
          set -euo pipefail
          cargo +nightly-2025-10-14 build \
            --ignore-rust-version \
            --release \
            --manifest-path compiler/codegen-backend/Cargo.toml

      - name: Require causal external managed assembly consumption
        shell: bash
        run: |
          set -euo pipefail
          python eng/verification/verify_external_managed_assembly_backend.py \
            --toolchain nightly-2025-10-14 \
            --backend "$GITHUB_WORKSPACE/compiler/codegen-backend/target/release/${{ matrix.backend }}"
