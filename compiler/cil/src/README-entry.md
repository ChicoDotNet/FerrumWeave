# Executable projection boundary

`executable_export.rs` is the first product-path CIL emitter that combines a real managed executable entry point with preserved managed API exports. It intentionally supports only the smallest R10 entry contract; broader source semantics remain compiler-lowering work.
